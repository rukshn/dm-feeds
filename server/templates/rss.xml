<?xml version="1.0" encoding="UTF-8"?>
<rss version="2.0">
<channel>
    <title>{{ website_name }}</title>
    <link>{{ domain_name }}</link>
    <description>
      RSS Feed of {{ website_name }} generated by Feed Burner (c) Rukshan Ranatunge - rukshan@ruky.me
    </description>
    <docs>https://github.com/rukshn</docs>
    <image>
      <url>{{ icon }}</url>
      <title>{{ website_name }}</title>
      <link>{{ domain_name }}</link>
      <width>32</width>
      <height>32</height>
    </image> 
    {% for article in articles %}
      <item>
        <title>{{ article.title }}</title>
        <link>{{ article.url }}</link>
        <pubDate>{{ article.updated }}</pubDate>
        <description>{{ article.content }}</description>
      </item>
    {% endfor % }
</channel>
</rss>